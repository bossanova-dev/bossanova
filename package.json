{
  "name": "bossanova",
  "private": true,
  "description": "Prettier for markdown formatting only",
  "devDependencies": {
    "prettier": "^3.4.0"
  },
  "scripts": {
    "format:docs": "prettier --write 'docs/**/*.md' '.claude/**/*.md' 'AGENTS.md'"
  }
}
