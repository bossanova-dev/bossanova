// The slug helper is portable and ships in boss-plan's toolbox; retain this
// compatibility re-export for callers that previously imported this module.
import { issueSlug } from '../skills-toolbox/plan-slug.mjs'

export { issueSlug }
